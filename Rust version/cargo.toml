[package]
name = "amm_aggregator"
version = "0.1.0"
edition = "2021"

[dependencies]
ic-cdk = "0.3.0"
ic-cdk-macros = "0.3.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
reqwest = { version = "0.11", features = ["json"] }

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "z"
￼Enter
